---
description: Configuration file conventions
globs: config/**
alwaysApply: false
---

# Config Conventions

All hardware parameters (CAN IDs, joint limits, gear ratios, COM ports) live in `config/robot.yaml`. Code should read from config rather than hardcoding values.

Joint limits are in radians. CAN IDs set to `null` mean "not yet assigned."
